<div class="row">
    <div class="col-3">
        <h2>Statistics</h2>

        <table class="table">
        <% @stats.keys.each do |s| %>
            <tr><td><%=s %></td><td><%= @stats[s]%></td>
        <% end %>
        </table>
    </div>
</div>
</div>
